import { GetErrorMassagePipe } from './get-error-massage.pipe';

describe('GetErrorMassagePipe', () => {
    it('create an instance', () => {
        const pipe = new GetErrorMassagePipe();

        expect(pipe).toBeTruthy();
    });
});
